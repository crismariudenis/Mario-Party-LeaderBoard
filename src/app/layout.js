export const metadata = {
  title: "Marciuc Party",
  description: "Mario Part Leaderboard",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{}}>{children}</body>
    </html>
  );
}
